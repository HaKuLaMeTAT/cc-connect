# Project Rules

- If asked to restart `cc-connect`, do not stop after issuing the restart command. Verify that `cc-connect` starts successfully and keeps running without runtime errors.
- If restart exposes any startup or runtime error, continue diagnosing and fixing until `cc-connect` runs normally, unless the user explicitly tells you to stop.
